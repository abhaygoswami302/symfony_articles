{% extends('base.html.twig') %}

{% block body  %}
    <div class="col-sm-6 offset-3 p-2">
        <div class="card">
        <div class="card-header">Post Create</div>
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </div>
    <div class="col-sm-3"></div>
{% endblock %}